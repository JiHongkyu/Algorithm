//방법 1
function solution(n) {
    let num = [];
    
    for(let i = 1; i <= n; i++) {
        if(i%2 === 1) {
            num.push(i);
        }
    }
    return num;
}


//방법 2
function solution(numbers) {
    return numbers.reduce( (total, current) => total + current, 0) / numbers.length;
}

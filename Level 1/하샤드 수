function solution(x) {
    var answer = true;
    var str = x.toString();
    var sum = 0;
    
    for(let i in str) {
        sum += parseInt(str[i])    ;
    }
    
    if(x%sum !== 0) answer = false;
    
    return answer;
}

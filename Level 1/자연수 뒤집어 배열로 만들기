function solution(n) {
    var answer = [];
    var str = n+"";
    
    for(let i = str.length-1; i >= 0; i--) {
        answer.push(parseInt(str[i]));
    }
    
    return answer;
}

// 1. n을 문자열로 만든 후 뒤에서부터 answer에 저장

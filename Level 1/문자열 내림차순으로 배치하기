function solution(s) {
    let lower = s.replace(/[A-Z]/g, '').split("").sort().reverse().join("");
    let upper = s.replace(/[a-z]/g, '').split("").sort().reverse().join("");
    
    return lower + upper;
}

// 1. 소문자, 대문자 따로 정렬
// 2. 소문자 뒤에 대문자 붙이고 리턴
